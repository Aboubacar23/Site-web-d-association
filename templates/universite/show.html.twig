{% extends 'base_admin.html.twig' %}

{% block title %}Universite{% endblock %}

{% block body %}
    <h1>Universite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ universite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ universite.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('universite_index') }}"><span class="btn btn-info">Retour</span></a>
    <br>
    <br>
    <a href="{{ path('universite_edit', {'id': universite.id}) }}" class="btn btn-primary"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>
    <br>
    <br>

    {{ include('universite/_delete_form.html.twig') }}
{% endblock %}
