  <div class="row">
                            {% for bureau in bureaux %}
                                <div class="col-md-3">
                                    <div class="card md-3">
                                        <div class="card-body">
                                            <h4 class="card-title">{{ bureau.nom }}</h4>
                                            <h3 class="card-title"> {{ bureau.prenom }}</h3>
                                            <div class="text-primary">
                                                <p>{{ bureau.pays }}</p>
                                            </div>
                                        </div>
                                    </div>
                                </div> 
                            {% endfor %}
        
                        </div>
 
{% for projet in user_projet %}
                        <div class="blog_left_sidebar">
                            <div class="blog_details">
                                     
                                <h4>{{ projet.Nom }}</h4>
                                <p>{{ projet.type.Nom }}</p>
                                <span><a href="{{path('voir_projet',{'id': projet.id})}}" class="text-danger">
                                Savoir plus</i></a></span>
                           
                            </div>
                        </div>
                        
                         {% endfor %}
                         
                         {{ knp_pagination_render( user_projet )}}