{% extends 'base_admin.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ genre.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genre_index') }}"><span class="btn btn-info">Retour</span></a>

    <a href="{{ path('genre_edit', {'id': genre.id}) }}" class="btn btn-primary"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
