{% extends 'base_admin.html.twig' %}

{% block title %}Mission{% endblock %}

{% block body %}
    <h1>Mission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mission.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ mission.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mission_index') }}"><span class="btn btn-info">Retour</span></a>

    <a href="{{ path('mission_edit', {'id': mission.id}) }}" class="btn btn-primary"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>

    {{ include('mission/_delete_form.html.twig') }}
{% endblock %}
