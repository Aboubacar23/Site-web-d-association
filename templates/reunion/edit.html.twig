{% extends 'base_admin.html.twig' %}

{% block title %}Edit Reunion{% endblock %}

{% block body %}
    <h1>Modifier la réunion</h1>

    {{ include('reunion/_form.html.twig', {'button_label': 'Modifier'}) }}
    <br>
    <br>
    <a href="{{ path('reunion_index') }}" class="btn btn-primary">Retour à la liste</a>
    <br>
    <br>
    {{ include('reunion/_delete_form.html.twig') }}
{% endblock %}
