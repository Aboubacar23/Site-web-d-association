{% extends 'base_admin.html.twig' %}

{% block title %}Reunion{% endblock %}

{% block body %}
    <h1>Reunion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reunion.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ reunion.Titre }}</td>
            </tr>
            <tr>
                <th>DateReunion</th>
                <td>{{ reunion.DateReunion ? reunion.DateReunion|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ reunion.HeureDebut ? reunion.HeureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HeureFin</th>
                <td>{{ reunion.DateFin ? reunion.DateFin|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reunion.Description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td><img src="{{asset('photo_reunion/' ~ reunion.Photo)}}" width="100px" height="70px;"></td>
            </tr>
        </tbody>
    </table>
    <br>
    <a href="{{ path('reunion_index') }}" class="btn btn-primary">Retour à la liste</a>
    <br>
    <br>
    <a href="{{ path('reunion_edit', {'id': reunion.id}) }}" class="btn btn-primary">edit</a>
    <br>
    <br>
    {{ include('reunion/_delete_form.html.twig') }}
{% endblock %}
